#!/bin/sh -e
LDFLAGS="-static" \
CFLAGS="-static" \
./configure \
    --prefix=/ \
    --disable-nls \
    --with-pic \
    --enable-shared \
    --enable-static

make 
make DESTDIR="$1" CFLAGS="-static" install

